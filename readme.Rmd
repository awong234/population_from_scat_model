---
output:
  html_document:
    keep_md: yes
---

# Population estimation from unstructured scat surveys

Author: Alec Wong
Compiled: `r format(Sys.time(), format = '%m-%d-%Y')`


```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = F, comment = NULL)

source('functions.R')

library(ggplot2)
library(dplyr)
library(foreach)
library(rgdal)

options(digits = 3)
```

## Analysis and simulation

Included are files to 